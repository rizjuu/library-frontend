function Footer() {
  return (
    <div
      style={{
        padding: "24px 0",
        textAlign: "center",
        color: "var(--text-muted)",
        fontSize: "12px",
      }}
    >
      Library Management System — Powered by React + Vite + MongoDB
    </div>
  );
}

export default Footer;
